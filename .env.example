# Ahrefs API v3 key (Bearer token). Required for the Ahrefs stage of
# scripts/collect-keywords.ts. If unset, the script skips Ahrefs and runs
# with Google Autocomplete only (all metric columns will be null).
AHREFS_API_KEY=
